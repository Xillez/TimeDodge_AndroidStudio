package com.example.timedodge.game;

import android.content.Context;

public class GameMode
{
    public static void loadGamemode(Context context, GameManager gameManager)
    {
        //XmlResourceParser parser = context.getResources().getXml(R.raw.gamemode_default);
    }
}
